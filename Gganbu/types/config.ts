/**
 * 项目开发配置
 */
export interface ProjectConfig {
  controllerDir?: string
  routerPrefix?: string
}

/**
 * server 的配置
 */
export interface ServerConfig {
  middlewares?: any[]
}

/**
 * server 的配置
 */
export interface ServerConfig {
  middlewares?: any[]
}
